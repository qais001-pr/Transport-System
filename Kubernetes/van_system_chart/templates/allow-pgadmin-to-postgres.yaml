{{- if .Values.networkPolicy.pgadminToPostgresIngress.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Values.networkPolicy.pgadminToPostgresIngress.name }}
spec:
  podSelector:
    matchLabels:
      app: {{ .Values.networkPolicy.pgadminToPostgresIngress.targetPod }}

  policyTypes:
    - Ingress

  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: {{ .Values.networkPolicy.pgadminToPostgresIngress.sourcePod }}
{{- end }}