{{- if .Values.networkPolicy.pgadminEgress.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Values.networkPolicy.pgadminEgress.name }}

spec:
  podSelector:
    matchLabels:
      app: {{ .Values.networkPolicy.pgadminEgress.sourcePod }}

  policyTypes:
    - Egress

  egress:
    - to:
        - podSelector:
            matchLabels:
              app: {{ .Values.networkPolicy.pgadminEgress.destinationPod }}
{{- end }}